#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
拡張リマインダー機能の使用ガイド
"""

from datetime import datetime, timedelta

def example_usage():
    """拡張リマインダー機能の使用例"""

    print("=" * 80)
    print("🎯 拡張リマインダー機能 - 使用ガイド")
    print("=" * 80)

    print("""
📋 **機能概要**
リマインダーメッセージが以下の機能で強化されました：

✅ **丁寧なメッセージ表現**
  • 時間帯に応じた適切な挨拶（おはようございます/お疲れ様です）
  • 敬語を使った丁寧な依頼表現
  • 状況に応じた謝罪や感謝の表現

✅ **詳細な天気情報統合**
  • MSN天気予報APIからリアルタイム天気情報を取得
  • 気温、降水確率、風情報などの詳細データ
  • 天気に応じた服装提案

✅ **緊急度別の色分け表示**
  • 本日: 赤色（緊急）
  • 明日: オレンジ色（重要）
  • 2日後以降: 青色/緑色（通常）

✅ **Flex Message対応**
  • 視覚的にわかりやすいカード表示
  • 天気情報付きのリッチメッセージ
  • モバイルフレンドリーなデザイン

📖 **使用方法**

1. **基本的なリマインダーメッセージ生成**
   ```python
   from enhanced_reminder_messages import generate_enhanced_reminder_message

   note_info = {
       "content": "イベント内容",
       "date": datetime(2025, 11, 15).date(),
       "days_until": 1,
       "is_input_deadline": False
   }

   message = generate_enhanced_reminder_message(note_info)
   print(message)
   ```

2. **Flex Message生成**
   ```python
   from enhanced_reminder_messages import generate_enhanced_flex_message

   flex_data = generate_enhanced_flex_message(note_info)
   # LINE Bot SDKでFlex Messageとして送信
   ```

3. **existing系統との統合**
   ```python
   from reminder_schedule import format_single_reminder_message, create_flex_reminder_message

   # 既存コードはそのまま動作し、自動的に拡張機能を使用
   message = format_single_reminder_message(note_dict)
   flex = create_flex_reminder_message(note_dict)
   ```

🔧 **技術仕様**

📁 **新規追加ファイル**
  • `src/enhanced_reminder_messages.py` - 拡張メッセージ生成エンジン

📝 **更新ファイル**
  • `src/reminder_schedule.py` - 既存関数を拡張版対応に更新

🌤️ **天気情報連携**
  • `uma3_custom_tools.py`のWeatherContextToolを活用
  • 自動的な地域判定と天気予報取得
  • 服装提案アルゴリズム

⚙️ **フォールバック機能**
  • 拡張モジュールが利用できない場合は基本版を使用
  • エラー時の安全な動作保証

🧪 **テスト**
  • `tests/test_enhanced_reminders.py` - 包括的なテストスイート
  • 複数シナリオでの動作確認済み

💡 **カスタマイズ**
  • メッセージテンプレートは`enhanced_reminder_messages.py`内で調整可能
  • 緊急度の色設定や表現は設定ファイルで変更可能
""")

    print("\n" + "=" * 80)
    print("✨ より丁寧で情報豊富なリマインダーをお楽しみください！")
    print("=" * 80)

if __name__ == "__main__":
    example_usage()

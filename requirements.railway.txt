# Railway最適化版 requirements.txt - 軽量化
# UMA3 LINE Bot用 - 基本機能版

# Core frameworks
flask==3.0.0

# LangChain core (必須のみ)
langchain==0.3.27
langchain-community==0.3.29
langchain-core==0.3.75
langchain-openai==0.3.3
langchain-chroma==0.1.4
langchain-text-splitters==0.3.11

# AI/ML libraries (軽量版)
openai==1.81.0
tiktoken==0.8.0

# Vector database
chromadb==0.5.23

# LINE Bot SDK
line-bot-sdk==3.15.0

# Data processing (軽量)
pandas==2.2.3
numpy>=1.26.0,<2.0.0

# Web handling
requests==2.32.5
httpx==0.28.1

# Environment & configuration
python-dotenv==1.1.1
pydantic==2.11.7

# Async support
aiohttp==3.12.15

# Utilities
tqdm==4.67.1
PyYAML==6.0.2
python-dateutil==2.9.0.post0
APScheduler==3.10.4

# Railway deployment
gunicorn==21.2.0

# Essential ML (CPU-only, lightweight)
scikit-learn==1.5.2

# Note: Heavy dependencies removed for Railway deployment
# - sentence-transformers (saves ~1GB)
# - transformers (saves ~1GB)
# - torch (saves ~2GB)
# - langchain-huggingface (saves ~500MB)
